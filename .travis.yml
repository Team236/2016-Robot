sudo: false
language: java
install: git clone https://github.com/Team236/wpilib-mirror.git ~/wpilib
script: ant jar
jdk: oraclejdk8

notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/f4cdfc44ab458ac747d2
    on_success: change  # options: [always|never|change] default: always
    on_failure: always  # options: [always|never|change] default: always
    on_start: never     # options: [always|never|change] default: always
